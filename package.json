{
    "scripts": {
        "build": "rimraf dist && tsc",
        "start": "yarn build && node dist"
    },
    "dependencies": {
        "axios": "^0.22.0",
        "body-parser": "^1.19.0",
        "express": "^4.17.1"
    },
    "devDependencies": {
        "@types/express": "^4.17.13",
        "@types/node": "^16.10.2",
        "rimraf": "^3.0.2",
        "typescript": "^4.4.3"
    }
}
