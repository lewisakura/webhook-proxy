{
    "scripts": {
        "update": "git pull && yarn build && pm2 restart webhook-proxy",
        "build": "prisma migrate deploy && prisma generate && rimraf dist && tsc",
        "start": "yarn build && node dist"
    },
    "dependencies": {
        "@prisma/client": "^3.2.1",
        "amqplib": "^0.8.0",
        "axios": "^0.22.0",
        "body-parser": "^1.19.0",
        "express": "^4.17.1",
        "express-async-errors": "^3.1.1",
        "express-slow-down": "^1.4.0",
        "helmet": "^4.6.0",
        "node-cache": "^5.1.2"
    },
    "devDependencies": {
        "@types/amqplib": "^0.8.2",
        "@types/express": "^4.17.13",
        "@types/express-slow-down": "^1.3.1",
        "@types/node": "^16.10.2",
        "prisma": "^3.2.1",
        "rimraf": "^3.0.2",
        "typescript": "^4.4.3"
    }
}
